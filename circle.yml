machine:
    timezone: Asia/Tokyo
    
    service:
        - docker

dependencies: 
  pre: 
    - chmod +x gradlew

deployment:
  chain:
    branch: circleci
    commands:
      curl -X POST https://circleci.com/api/v1/project/AkihikoOkubo/nablarch-wmq-adaptor/tree/master?circle-token=$nablarch_wmq_adaptor_TOKEN